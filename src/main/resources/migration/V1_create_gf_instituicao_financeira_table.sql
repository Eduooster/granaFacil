CREATE TABLE T_GF_INSTITUICAO_FINANCEIRA (
     ID              NUMBER(19) GENERATED BY DEFAULT ON NULL AS IDENTITY,
     ID_CONNECTOR    NUMBER(19)     NOT NULL,
     NAME            VARCHAR2(150)  NOT NULL,
     IMAGE_URL       VARCHAR2(500)  NULL,
     HEALTH          VARCHAR2(50)   NULL,
     TYPE_BANK       VARCHAR2(50)   NULL,

     CONSTRAINT PK_GF_INSTITUICAO_FINANCEIRA PRIMARY KEY (ID)
);
